# Windows Email Service Requirements
# For the REST API email service that runs on Windows

# Web framework and API
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Windows-specific (only install on Windows)
pywin32==306; sys_platform == "win32"

# HTTP client for main application
requests==2.31.0

# Additional utilities
python-multipart==0.0.6  # For file uploads if needed
aiofiles==23.2.1  # For async file operations

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2  # For testing FastAPI endpoints

# Service installation (Windows)
python-windows-service==1.0.0; sys_platform == "win32"

# Logging and monitoring
structlog==23.2.0
